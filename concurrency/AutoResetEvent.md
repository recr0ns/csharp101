#AutoResetEvent
¬ базовом варианте `AutoResetEvent` используетс€ дл€ отправки сигналов между двум€ потоками.

`AutoResetEvent` содержить булевый флаг. ≈сли он `false`, тогда мы блокируем поток, иначе разблокируем.

ѕоток может заблокировать свое состо€ние с помощью метода *WaitOne*. 
ѕри этом другой поток сможет его разблокировать с использованием метода *Set*.

##Examples
###Case 1
ƒелаем условный запрос на сервер, дл€ получени€ информации. » на врем€ его выполнени€ блокируем главный поток.
ѕосле того, как мы получили данные, то вызываем *Set*, что приводит к разблокированию главного потока.

###Case 2
«адача: один поток владеет списком четных чисел, второй - списком нечетных. Ќужно вывести в консоль данные поочередно из каждого списка.

ƒл€ этого будем использовать 2а экземпл€ра `AutoResetEven` и, "жонглирую" блокировками, реализуем необходимое поведение.
ѕервый поток выписывает одно число, после этого разблокирует второй поток и после этого блокируетс€, ожида€ внешнего сигнала.
¬торой поток, после разблокировани€, выписывает свое число и разблокирует первый поток, после чего сам блокируетс€.

_≈сли в наших списках будет не одинаковое количество элементов, то один из потоков заблокируетс€ навсегда, ожида€ команды от потока, который уже завершилс€_

###Case 3
ѕример, когда несколько потоков конкурируют за один `AutoResetEvent`.
Ќе известно какой из поток при разблокировании будет выполн€тьс€. ¬ итоге получили последовательное исполнение потоков с конкурентнстью за пор€док выполнени€.
